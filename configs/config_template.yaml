project:
  symbol: "XAUUSD"
  timeframe: "M5"
  start_year: 2013
  end_year: 2025

model:
  architecture: "TimesNet"
  window_length: 64
  stride: 1
  horizon: 1
  num_classes: 3
  seed: 42

evaluation:
  split: "walk_forward"
  metrics: ["accuracy", "balanced_accuracy", "f1", "mcc"]
  notes: "Public repo contains methodology only; private repo holds full pipeline."
